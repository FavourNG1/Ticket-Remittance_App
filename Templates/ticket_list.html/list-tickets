<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Ticket Remittance</title>
</head>
<body>
    <h1>Tickets</h1>
    <a href="{{ url_for('new_ticket') }}">Create New Ticket</a>
    <ul>
        {% for ticket in tickets %}
            <li>{{ ticket.event_name }} - {{ ticket.purchaser_name }} - {{ ticket.amount }} - {{ ticket.status }}
                {% if ticket.status == 'Pending' %}
                    <form action="{{ url_for('remit_ticket', id=ticket.id) }}" method="POST" style="display:inline;">
                        <button type="submit">Remit</button>
                    </form>
                {% endif %}
            </li>
        {% endfor %}
    </ul>
</body>
</html>
