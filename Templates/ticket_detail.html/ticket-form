<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>New Ticket</title>
</head>
<body>
    <h1>Create New Ticket</h1>
    <form method="POST">
        {{ form.hidden_tag() }}
        <p>
            {{ form.event_name.label }}<br>
            {{ form.event_name(size=32) }}
        </p>
        <p>
            {{ form.purchaser_name.label }}<br>
            {{ form.purchaser_name(size=32) }}
        </p>
        <p>
            {{ form.amount.label }}<br>
            {{ form.amount(size=32) }}
        </p>
        <p>{{ form.submit() }}</p>
    </form>
</body>
</html>
